name: CI Teste HTML/CSS + SAST + DAST

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # 🧱 Job 1 - Teste de verificação básica (CI)
  test:
    name: Teste CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Verificar arquivos existentes
        run: |
          echo "Verificando arquivos..."
          test -f index.html && echo "index.html encontrado ✅"
          test -f style.css && echo "style.css encontrado ✅"

      - name: Teste final
        run: echo "Pipeline concluído com sucesso! 🚀"

  # 🔍 Job 2 - Teste SAST (análise estática)
  sast:
    name: Teste SAST
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Executar SAST
        run: echo "Executando teste SAST 🔒"

  # 🌐 Job 3 - Teste DAST (análise dinâmica)
  dast:
    name: Teste DAST
    runs-on: ubuntu-latest
    needs: sast
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Executar DAST
        run: echo "Executando teste DAST 🌐"
